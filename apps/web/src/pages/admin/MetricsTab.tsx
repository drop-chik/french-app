// Phase 2 fills this with the metrics dashboard (DAU/WAU/MAU, retention,
// feature usage). Placeholder for now so the Users tab can ship first.
import styles from './AdminPage.module.css';

export function MetricsTab() {
  return (
    <div className={styles.metricsPlaceholder}>
      Метрики появятся в следующем шаге.
    </div>
  );
}
